function clamp(n,min,max){return Math.max(min,Math.min(max,n))} function weatherAdjustment(w){if(!w||w.dome)return 0;let a=0,wind=Number(w.wind||0),temp=Number(w.temp||70),p=Number(w.precip||0);if(wind>=20)a-=3.5;else if(wind>=15)a-=2.25;else if(wind>=12)a-=1.25;if(p>=70)a-=1;if(temp>=93)a-=.4;if(temp<=32)a-=.8;return a} export function analyzeGame(g){const market=Number(g.total),a=g.awayProfile,h=g.homeProfile;let statAdj=0,notes=[];if(a&&h){const raw=((a.pf+h.pa)/2)+((h.pf+a.pa)/2);statAdj=clamp((raw-market)*.35,-5,5);notes.push(`recent scoring model ${raw.toFixed(1)}`)}else notes.push("limited recent-season sample");const w=weatherAdjustment(g.weather),projected=market+statAdj+w,edge=projected-market,side=edge>=0?"OVER":"UNDER",altLine=side==="OVER"?market-2.5:market+2.5,abs=Math.abs(edge),confidence=abs>=3.5?"High":abs>=1.75?"Medium":"Low";if(w<0)notes.push(`weather ${w.toFixed(1)}`);else if(g.weather?.dome)notes.push("indoor weather neutral");else notes.push("weather neutral");return{...g,projected:Number(projected.toFixed(1)),edge:Number(edge.toFixed(1)),side,altLine:Number(altLine.toFixed(1)),confidence,modelNote:notes.join(" • ")}}