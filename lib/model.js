function clamp(n,min,max){return Math.max(min,Math.min(max,n))}
function weatherAdjustment(w){if(!w||w.dome)return 0;let a=0,wind=Number(w.wind||0),temp=Number(w.temp||70);if(wind>=20)a-=3;else if(wind>=15)a-=2;else if(wind>=12)a-=1;if(temp>=93)a-=.5;if(temp<=32)a-=.75;return a}
export function analyzeGame(g){const market=Number(g.total),recent=(g.recent||[]).filter(Number.isFinite),avg=recent.length?recent.reduce((a,b)=>a+b,0)/recent.length:market,recentSignal=clamp((avg-market)*.18,-4,4),w=weatherAdjustment(g.weather),projected=market+recentSignal+w,edge=projected-market,side=edge>=0?"OVER":"UNDER",altLine=side==="OVER"?market-2.5:market+2.5,abs=Math.abs(edge),confidence=abs>=3.25?"High":abs>=1.5?"Medium":"Low",reasons=[];if(recent.length)reasons.push(`recent game-total signal ${avg.toFixed(1)}`);if(w<0)reasons.push(`weather adjustment ${w.toFixed(1)}`);else if(g.weather?.dome)reasons.push("weather neutral indoors");else reasons.push("no major weather penalty");return {...g,projected:Number(projected.toFixed(1)),edge:Number(edge.toFixed(1)),side,altLine:Number(altLine.toFixed(1)),confidence,modelNote:reasons.join(" • ")}}
