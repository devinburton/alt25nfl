import{STADIUMS,DOME_TEAMS}from"./stadiums"; export async function getGameWeather(home,kickoff){if(DOME_TEAMS.has(home))return{label:"Indoor / dome",dome:true,temp:null,wind:0,precip:0};const c=STADIUMS[home];if(!c)return{label:"Weather unavailable",dome:false};const[lat,lon]=c,d=new Date(kickoff),date=d.toISOString().slice(0,10),u=new URL("https://api.open-meteo.com/v1/forecast");u.searchParams.set("latitude",lat);u.searchParams.set("longitude",lon);u.searchParams.set("hourly","temperature_2m,precipitation_probability,wind_speed_10m");u.searchParams.set("temperature_unit","fahrenheit");u.searchParams.set("wind_speed_unit","mph");u.searchParams.set("timezone","UTC");u.searchParams.set("start_date",date);u.searchParams.set("end_date",date);try{const r=await fetch(u,{next:{revalidate:3600}});if(!r.ok)throw 0;const x=await r.json(),ts=x.hourly?.time||[];let idx=0,best=Infinity;ts.forEach((t,i)=>{const dif=Math.abs(new Date(t+"Z").getTime()-d.getTime());if(dif<best){best=dif;idx=i}});const temp=x.hourly?.temperature_2m?.[idx],wind=x.hourly?.wind_speed_10m?.[idx],precip=x.hourly?.precipitation_probability?.[idx];if(!Number.isFinite(temp))return{label:"Weather pending",dome:false};return{label:`${Math.round(temp)}°F • ${Math.round(wind||0)} mph • ${Math.round(precip||0)}% rain`,dome:false,temp,wind:wind||0,precip:precip||0}}catch{return{label:"Weather pending",dome:false}}}