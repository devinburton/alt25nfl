import{NextResponse}from"next/server";import{getCurrentWeekBundle,getSeasonScoreboard,getInjuriesForTeam}from"../../../lib/espn";import{fetchLiveNflTotals}from"../../../lib/liveOdds";import{getGameWeather}from"../../../lib/weather";import{buildTeamProfile,injuryAdjustment}from"../../../lib/analytics";import{analyzeGame}from"../../../lib/model";
export const dynamic="force-dynamic";const norm=(s="")=>s.toLowerCase().replace(/[^a-z0-9]/g,"");
export async function GET(){try{const current=await getCurrentWeekBundle();const[seasonGames,odds]=await Promise.all([getSeasonScoreboard(current.year,current.seasonType),fetchLiveNflTotals()]);const om=new Map(odds.map(o=>[`${norm(o.away)}-${norm(o.home)}`,o]));const rows=await Promise.all(current.events.filter(g=>g.week===current.week||!g.week).map(async s=>{const o=om.get(`${norm(s.away)}-${norm(s.home)}`);if(!o)return null;const[weather,awayProfile,homeProfile,awayInj,homeInj]=await Promise.all([getGameWeather(s.home,s.kickoff),buildTeamProfile(s.away,s.awayAbbr,seasonGames,s.kickoff),buildTeamProfile(s.home,s.homeAbbr,seasonGames,s.kickoff),getInjuriesForTeam(s.awayAbbr),getInjuriesForTeam(s.homeAbbr)]);return analyzeGame({...s,total:o.total,booksCount:o.booksCount,weather,awayProfile,homeProfile,awayInjury:injuryAdjustment(awayInj),homeInjury:injuryAdjustment(homeInj)})}));const games=rows.filter(Boolean).sort((a,b)=>new Date(a.kickoff)-new Date(b.kickoff));return NextResponse.json({updatedAt:new Date().toISOString(),year:current.year,week:current.week,oddsMode:"LIVE CONSENSUS",modelVersion:"final-v1",altRule:2.5,games})}catch(e){return NextResponse.json({error:e.message||"Unable to build board"},{status:500})}}
