"use client";
import{useEffect,useMemo,useState}from"react";
function fmt(v){const d=new Date(v);return new Intl.DateTimeFormat("en-US",{weekday:"short",month:"short",day:"numeric",hour:"numeric",minute:"2-digit",timeZone:"America/New_York"}).format(d)+" ET"}
export default function Home(){const[data,setData]=useState(null),[error,setError]=useState(""),[filter,setFilter]=useState("ALL");useEffect(()=>{fetch("/api/board",{cache:"no-store"}).then(r=>{if(!r.ok)throw new Error("Could not load the board.");return r.json()}).then(setData).catch(e=>setError(e.message))},[]);const games=useMemo(()=>{const l=data?.games||[];return filter==="ALL"?l:l.filter(g=>g.side===filter)},[data,filter]);return <main className="container"><section className="hero"><div><div className="eyebrow">NFL TOTALS ONLY</div><h1>ALT 2.5</h1><p className="sub">Start with the market total, analyze the game, choose one side, then move exactly 2.5 points in our favor. No spreads. No moneylines. No props.</p></div><div className="status">{data?`${data.oddsMode} • model ${data.modelVersion}`:"Loading board…"}</div></section><div className="toolbar">{["ALL","OVER","UNDER"].map(x=><button key={x} className={`filter ${filter===x?"active":""}`} onClick={()=>setFilter(x)} type="button">{x==="ALL"?"All games":`${x} picks`}</button>)}</div>{error&&<div className="error">{error}</div>}{!data&&!error&&<div className="loading">Building this week's totals board…</div>}{data&&<section className="grid">{games.map((g,i)=><article className="card" key={`${g.away}-${g.home}-${i}`}><div className="topline"><div><div className="time">{fmt(g.kickoff)}</div><div className="matchup">{g.away} @ {g.home}</div></div><div className="badge">{g.weather?.label||"Weather pending"}</div></div><div className="metrics"><div className="metric"><span>Vegas total</span><strong>{g.total.toFixed(1)}</strong></div><div className="metric"><span>Projection</span><strong>{g.projected.toFixed(1)}</strong></div><div className="metric"><span>Edge</span><strong>{g.edge>0?"+":""}{g.edge.toFixed(1)}</strong></div></div><div className="pickbox"><div className="pickline"><div className={`pick ${g.side.toLowerCase()}`}>{g.side}</div><div className="alt">Group line: {g.side} {g.altLine.toFixed(1)}</div></div><div className="row" style={{marginTop:8}}><span className="badge">Confidence: {g.confidence}</span>{g.liveOdds&&<span className="badge">{g.booksCount} books</span>}</div><p className="reason">{g.modelNote}</p></div></article>)}</section>}<div className="footer-note"><strong>Important:</strong> this starter model is intentionally transparent and simple. Live odds can replace the snapshot with an API key. Before treating picks as predictive-grade, add a stronger season-stat feed and back-test the formula. Betting involves risk.</div></main>}
